# Auto detect text files and perform LF normalization
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.swift text eol=lf
*.md text eol=lf
*.json text eol=lf
*.ts text eol=lf
*.html text eol=lf
*.sh text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary

# Entitlements files should be treated as text
# Note: If you get "entitlements were modified during the build" errors,
# remove extended attributes with: xattr -c FitHub.entitlements
*.entitlements text

